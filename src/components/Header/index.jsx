import React from 'react';

const Header = (props) => {

    return (
        <header className="ma-header">
            <div>Log in</div>
            <div>Sign up</div>
        </header>
    )
};

export default Header;